# ProspectaFluxus — exemplo seguro de variáveis de ambiente
# Copie apenas os nomes necessários para o painel do Railway. Nunca grave valores reais no Git.

# Aplicação
NODE_ENV=production
PORT=3000

# Banco PostgreSQL
DATABASE_URL=postgresql://USUARIO:SENHA@HOST:5432/BANCO

# Sessão e tarefas agendadas
# Gere valores diferentes com: openssl rand -base64 48
JWT_SECRET=SUBSTITUA_POR_UM_SEGREDO_NOVO
CRON_SECRET=SUBSTITUA_POR_OUTRO_SEGREDO_NOVO

# IA para sugestões de follow-up (opcional)
GEMINI_API_KEY=
OPENROUTER_API_KEY=

# Email de lembrete (opcional)
RESEND_API_KEY=
NOTIFICATION_EMAIL=

# Web Push — obrigatório para alertas com o navegador fechado
# Gere o par uma única vez com: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:SEU_EMAIL_DE_SUPORTE

# Cifragem das credenciais Trello — obrigatório para ativar a integração
# Gere 32 bytes em hexadecimal com: openssl rand -hex 32
# Não altere esta chave depois de guardar credenciais Trello, ou será necessário ligá-las novamente.
INTEGRATION_ENCRYPTION_KEY=

# Armazenamento privado dos áudios no Railway — S3/R2/compatível
S3_BUCKET=
S3_REGION=auto
S3_ENDPOINT=
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
S3_FORCE_PATH_STYLE=false

# Infraestrutura integrada legada (deixe vazia no Railway quando usar S3)
VITE_APP_ID=
OAUTH_SERVER_URL=
OWNER_OPEN_ID=
BUILT_IN_FORGE_API_URL=
BUILT_IN_FORGE_API_KEY=
VITE_FRONTEND_FORGE_API_KEY=
VITE_FRONTEND_FORGE_API_URL=
