# ─── ProspectaFluxus — Variáveis de Ambiente ─────────────────────────────────
# Copie este conteúdo para um arquivo .env local ou configure no painel do Render.

# ─── Banco de Dados (Supabase PostgreSQL) ─────────────────────────────────────
# Formato: postgresql://postgres:SENHA@db.PROJETO.supabase.co:5432/postgres
DATABASE_URL=postgresql://postgres:SUA_SENHA@db.blqtjvftkamzofpltlrj.supabase.co:5432/postgres

# ─── Autenticação JWT ─────────────────────────────────────────────────────────
# Gere um valor aleatório seguro: openssl rand -base64 32
JWT_SECRET=gere-um-valor-aleatorio-seguro-aqui

# ─── Google Gemini (IA para sugestões de follow-up) ──────────────────────────
# Obtenha em: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=sua-chave-gemini-aqui

# ─── Render Cron Jobs (segurança dos endpoints de agendamento) ───────────────
# Gere um valor aleatório seguro: openssl rand -base64 32
CRON_SECRET=gere-outro-valor-aleatorio-seguro

# ─── Notificações por Email (opcional — via Resend) ──────────────────────────
# Obtenha em: https://resend.com (plano gratuito: 3.000 emails/mês)
RESEND_API_KEY=re_sua_chave_resend_aqui
NOTIFICATION_EMAIL=michelle@innoflow.com.br

# ─── Variáveis legadas do Manus (deixar vazias para deploy independente) ──────
VITE_APP_ID=
OAUTH_SERVER_URL=
OWNER_OPEN_ID=
BUILT_IN_FORGE_API_URL=
BUILT_IN_FORGE_API_KEY=
VITE_FRONTEND_FORGE_API_KEY=
VITE_FRONTEND_FORGE_API_URL=
